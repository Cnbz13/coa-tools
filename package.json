{
  "name": "coa-tools",
  "version": "1.5.6",
  "private": true,
  "type": "module",
  "description": "Local companion suite for combat, UI and addon management.",
  "engines": {
    "node": ">=24.14.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "watch:coa": "node scripts/coa-watch.mjs",
    "validate:manifest": "node scripts/validate-manifest.mjs",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "luaparse": "0.3.1"
  }
}
